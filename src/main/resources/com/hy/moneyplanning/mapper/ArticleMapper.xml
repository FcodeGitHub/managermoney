<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hy.moneyplanning.mapper.ArticleMapper">
    <select id="showInfo" parameterType="int" resultType="com.hy.moneyplanning.pojo.Notice">
        select * from notice where id = #{id}
    </select>

</mapper>